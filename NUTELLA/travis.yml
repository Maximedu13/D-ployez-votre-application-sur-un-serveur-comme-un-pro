language: python
python:
  - '3.7.6'

before_script:
  - pip install -r requirements.txt

env: DJANGO_SETTINGS_MODULE="nutella_stop.settings"

services:
  - postgresql

script:
  - ./manage.py test

# safelist
branches:
  only:
    - staging
